#include <iostream>

using namespace std;

int main(){
    long long a, b, c;
    long long result = 0;
    cin >> a >> b >> c;
    result = a + b + c;
    cout << result;
    
    return 0;
}